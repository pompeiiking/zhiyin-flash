# 全功能测试用例与结果矩阵

测试窗口：2026-09-24～25；源码 `main@635fcb6` 加本地候选修改，项目版本为 `1.0.1`。唯一测试入口：最新源码 API `http://127.0.0.1:18000`、Vite `http://127.0.0.1:5174`、独立 PostgreSQL 库 `zhiyin_audit` 与独立 Redis 前缀 `zhiyin_audit`。旧 Docker `:8000` 是旧镜像，不参与候选结果。所有账号为随机合成账号；未把密钥、令牌、真实个人资料写进仓库。

证据缩写：**S** = `tests/e2e/api_surface_verify.py`（43/43）；**L** = `loop_verify.py`（35/35）；**B** = `full_path.py`（81/81）；**A** = `ai_tasks_verify.py`（九任务先前独立三轮各 9/9，R12 同版一轮 9/9）；**C** = `chart_probe.py`（早期独立五轮出图 5/5，画像上下文修复后连续四轮各八项断言通过）；**U** = `pytest`（全量 477/477）；**N** = 未登录请求矩阵（42 次，其中 39 次为拒绝或入参校验，登出与情报读/刷新按公开口径返回）。`linkage_probe.py` 为 4/4，`topbar_probe.py` 通过。浏览器脚本的截图和 JSON 结果在 `tests/e2e/` 下，原始日志仅保留在本机测试目录；其中不包含 API 密钥。

## 46 个契约方法逐项核对

`通过`表示已看到候选源码真实服务的成功/预期空态回执，并由 N 或 U 覆盖错误边界；读接口的有数据态由 L/B 覆盖。`条件`表示端点已调用，但外部有效凭据或真实事件未满足，不能把正向链路算作通过。一个 S 检查可能包含多次请求；下表按 OpenAPI 的方法逐行列出。

| ID | 功能 | 方法和路径 | 实测证据 | 结论 |
| --- | --- | --- | --- | --- |
| 01 | F06 | DELETE `/api/v1/app/academic` | S、N、U | 通过 |
| 02 | F06 | POST `/api/v1/app/academic/import` | S、N、U | 通过 |
| 03 | F06 | POST `/api/v1/app/academic/import/file` | S、B、N、U | 通过 |
| 04 | F15 | GET `/api/v1/app/achievements` | S、B、N、U | 通过 |
| 05 | F07 | POST `/api/v1/app/assets/export` | S、N、U | 条件：接口如实返回 `available=false`，正式导出未实现 |
| 06 | F07 | GET `/api/v1/app/assets/{asset_type}/versions` | S、L、B、N | 通过 |
| 07 | F02 | POST `/api/v1/app/auth/login` | S、B、U | 通过 |
| 08 | F02 | POST `/api/v1/app/auth/logout` | S、B、U | 通过；无令牌登出为空操作 |
| 09 | F02 | POST `/api/v1/app/auth/register` | S、L、B、U | 通过 |
| 10 | F01 | GET `/api/v1/app/bootstrap` | S、B、N | 通过 |
| 11 | F11 | POST `/api/v1/app/brief/today` | A、B、N | 通过 |
| 12 | F10 | GET `/api/v1/app/calendar` | S、L、B、N | 通过 |
| 13 | F05 | POST `/api/v1/app/chsi/bind` | 无效验证码返回 SSE 错误、N、U | 条件：缺真实有效在线验证码，未验成功绑定 |
| 14 | F01 | POST `/api/v1/app/config/reload` | S、N | 通过；当前已登录用户均可调用，见 D04 |
| 15 | F04 | POST `/api/v1/app/conversation/material` | S（有效文本/缺文件）、N、U | 通过 |
| 16 | F04 | POST `/api/v1/app/conversation/message` | L、B、C、N、U | 通过 |
| 17 | F11 | POST `/api/v1/app/day/{day}/advice` | A、N、U | 通过 |
| 18 | F08 | POST `/api/v1/app/dimensions/{dimension_id}` | A、B、N | 通过 |
| 19 | F08 | POST `/api/v1/app/gaps/{gap_key}/clarify` | A、N | 通过 |
| 20 | F12 | GET `/api/v1/app/intel` | S、B、公开访问检查 | 通过 |
| 21 | F12 | POST `/api/v1/app/intel/refresh` | B、公开访问检查 | 通过 |
| 22 | F08 | POST `/api/v1/app/match/careers` | A、B、N | 通过 |
| 23 | F13 | GET `/api/v1/app/notes` | S、B、N | 通过 |
| 24 | F13 | POST `/api/v1/app/notes` | S、B、N | 通过 |
| 25 | F13 | DELETE `/api/v1/app/notes/{note_id}` | S、N、U | 通过 |
| 26 | F13 | PATCH `/api/v1/app/notes/{note_id}` | S、N、U | 通过 |
| 27 | F14 | GET `/api/v1/app/notifications/pending` | S、B、N | 通过 |
| 28 | F14 | POST `/api/v1/app/notifications/{message_id}/read` | S（不存在 ID 回 `read=0`）、隔离库真实待处理通知：本人已读后从 2 条减为 1 条，跨账号 `read=false`、N、U | 通过；重复已读返回 `read=true` 但列表不重复 |
| 29 | F10 | GET `/api/v1/app/plan/action` | S、L、B、N | 通过 |
| 30 | F10 | PATCH `/api/v1/app/plan/action/tasks` | L、B、联动 4/4、N | 通过，含勾选与撤回 |
| 31 | F09 | GET `/api/v1/app/plan/directions` | S、L、B、N | 通过 |
| 32 | F09 | POST `/api/v1/app/plan/directions/{option_id}/select` | L、B、N | 通过 |
| 33 | F10 | POST `/api/v1/app/plan/timetable` | A、B、N | 通过 |
| 34 | F10 | POST `/api/v1/app/plan/todos/suggestions` | A、N | 通过 |
| 35 | F03 | GET `/api/v1/app/portal` | S、B、公开空态 | 通过 |
| 36 | F08 | POST `/api/v1/app/portrait/analysis` | A、N | 通过 |
| 37 | F07 | GET `/api/v1/app/report/full-text` | S、L、B、N | 通过 |
| 38 | F07 | POST `/api/v1/app/report/summary` | A、B、N | 通过 |
| 39 | F03 | GET `/api/v1/app/sessions` | S、B、N | 通过 |
| 40 | F03 | GET `/api/v1/app/sessions/{task_id}/turns` | S、L、B、N | 通过，跨账号拒绝 |
| 41 | F03 | POST `/api/v1/app/task/enter` | S（七入口+未知）、L、B、N | 通过 |
| 42 | F12 | GET `/api/v1/app/theory-cards/{theory_id}` | S、B、N | 通过 |
| 43 | F15 | POST `/api/v1/app/track` | S（同 ID 重放）、B、N | 通过 |
| 44 | F15 | GET `/api/v1/app/track/events` | S、B、N | 通过 |
| 45 | F05 | GET `/api/v1/app/workspace` | S、L、B、N | 通过 |
| 46 | F01 | GET `/healthz` | S | 通过 |

## 轮次与未覆盖项

| 轮次 | 完成证据 | 尚不能宣称覆盖的内容 |
| --- | --- | --- |
| T0 版本冻结 | 远端 `origin/main` 与本地 HEAD 同为 `635fcb6`；OpenAPI 44 路径/46 方法；候选服务健康；26 条提示词文件与隔离库内容、状态逐条哈希相同 | 旧 Docker 镜像仍滞后；正式镜像 digest/发布域名未替换 |
| T1 静态与单元 | 477/477、Ruff 通过；对象存储写失败专项 6/6、OpenAPI 快照一致、Vue 类型检查与构建通过 | Starlette/httpx 弃用警告，前端打包体积警告 |
| T2 模块与契约 | S 43/43；上表 46 方法逐项确认；未登录矩阵 42 次；七入口全部成功；真实通知已读/跨账号校验 | 学信成功绑定、正式导出见例外；并非每种无效入参都逐个排列组合 |
| T3 真实依赖 | DeepSeek 五环节与九任务；DashScope 实测 1×1024 维；pgvector 0.8.6、隔离库 6 条 1024 维向量；学职平台取回来源；无效学信码为错误终帧 | 无有效学信验证码；对象存储写失败、外部数据超时与向量失败已在契约/单元层注入；未在隔离服务做磁盘故障注入；限流业务端点未接线，见 D15 |
| T4 跨功能 | L 35/35、A 先前三轮各 9/9、R12 同版一轮 9/9，C 最终连续四轮各 8/8（含已有画像不误报空白、项目名称不误触发）；版本、缓存失效、任务撤回、幂等与越权 | 固定合成语料中九任务完整/缺失/冲突各三轮均已覆盖；10 组匿名 A/B 已按用户要求由 Codex 自评；独立真人复核未做 |
| T5 浏览器 | B 81/81、联动 4/4、顶栏探针通过；桌面/375px、三页面、图表与错误态 | 关闭功能开关后的界面门控尚有缺口，见 D03 |
| T6 稳定性 | 50 次并发跨账号笔记读取 50/50 隔离；同 `client_msg_id` 重放不再跑模型；服务重启后续接；两类 2,000,001 字节文件均 422；浏览器断网注入后页面非空、恢复后工作台 200；相邻两日建议分别回填日期且缓存键分离，同日 `+480/-480` 分桶；完整回路 22 轮、浏览器 18 轮；外部数据/向量失败降级单测 | Redis 限流尚未接业务端点，见 D15；未进行持续数小时的负载压测 |

**核对结论：**候选代码的已测主链、46 方法接口和固定模型语料均有结果；浏览器最终回归 81/81、组件联动 4/4；10 组匿名 A/B 已由 Codex 按用户要求自评，候选 77/80、事实硬失败 0/10；评审者不是独立真人。模型最终主语料 201/201、扩展 51/51、高风险九例五轮 45/45，程序边界 297/297。原有未实现或关闭能力作为明确例外列在《缺陷清单.md》，不能算功能通过。例外与缺口见《缺陷清单.md》及《发布及回滚记录.md》。

浏览器脚本曾把欢迎词/“正在回你”占位当成正式答复，并把用户气泡错认成 `.line.user`（实际为 `.line.me`），导致一轮 79/82 的三条对话断言误报；已改为定位本轮用户气泡之后的正式 AI 回复。页面后台持续刷新情报，脚本也改为 DOM 就绪后按具体元素和资产核对，最终完整重跑 81/81。另有一次模型结构修复后的用户回复泄露内部“格式问题”措辞，处理与五轮专项探针见 D20；该次观察不混入最终固定语料分数。
