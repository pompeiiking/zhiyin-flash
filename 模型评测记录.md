# 模型评测记录

更新于 2026-09-25。基线 Prompt-R0 固定为 `main@635fcb6274126642e59cf1881c4804d4c6fcc4e6`，候选为本地 Prompt-R12。两版使用 DeepSeek `deepseek-chat`、相同输出契约与 `max_tokens=6000`，按各自提示词登记的温度交错运行；每份 JSON 最多允许一次结构修复。候选决策角色温度为 0。实验编号与项目版本分开，本次本地稳定模块的 `pyproject.toml` 为 `1.0.1`。输入均为合成案例，不含真实个人资料或密钥。

## 最终重复对照

| 语料 | 样本与重复 | R0 合约有效 | R12 合约有效 | R12 可程序判定事实边界 |
| --- | --- | ---: | ---: | ---: |
| 主语料 | 五环节 40 例、九页面任务 27 例；各 3 次 | 201/201 | 201/201 | 201/201 |
| 扩展语料 | 七入口及异常边界 17 例；各 3 次 | 51/51 | 51/51 | 51/51 |
| 高风险复测 | 采集情绪、诊断正常/矛盾、决策矛盾、复盘情绪、四个缺数据任务，共 9 例；各 5 次 | 44/45 | 45/45 | 45/45 |
| **合计** | **297 次/版本** | **296/297** | **297/297** | **297/297，0 条可判定违例** |

三份原始 JSONL 为 `zhiyin-src/template/tests/e2e/prompt_ab_final_v12_results.jsonl`、`prompt_ab_extra_v12_results.jsonl`、`prompt_ab_high_risk_v12_results.jsonl`。每行保留匿名输入、原始输出、模型名、提示词指纹、有效性、修复、耗时和 token 用量。早期关键失败原始行另见 `zhiyin-src/template/tests/e2e/prompt_ab_failure_samples.jsonl`，避免把整批调试输出误当最终指标。`prompt_ab_eval.py` 固定 R0 源码提交，`prompt_semantic_checks.py` 会把无效模型输出计为失败，并检查无来源分数、理论引用、排名、待办 ID、材料注入以及本次发现的情绪归因与示例串扰。三份合并执行结果为 `candidate_outputs=297 valid_outputs=297 invalid_outputs=0 semantic_failures=0`。

| 指标 | R0 | R12 |
| --- | ---: | ---: |
| 一次结构修复 | 13 | 2 |
| 总 token | 1,036,798 | 903,337 |
| 平均单请求耗时 | 3.43 秒 | 2.75 秒 |
| P95 单请求耗时 | 8.67 秒 | 4.70 秒 |

候选在本次固定语料下少用约 12.9% token。耗时不含排队和网络外的 UI 时间；没有按未知价格换算费用，也不外推生产成本或时延。基线有一份结构失败，候选在本批样本没有结构失败。自然语言事实边界仍需读样本，程序检查不是语义全知判定。

## 匿名 A/B 自评

根目录《盲评样本与评分表.md》保留 10 组完整匿名输入输出及逐项评分。用户明确要求由我判断并填写，因此评审者是 **Codex，AI 自评**，不是独立真人评审。我在揭示 A/B 版本映射前填写相关性、尊重、可执行、清楚四项各 0–2 分，以及事实硬失败和偏好。揭示后汇总：R12 为 **77/80 分、0/10 事实硬失败**；R0 为 **69/80 分、6/10 事实硬失败**；偏好 R12 六组、R0 四组。R0 有几组表达更简短或行动更直接，但包含无来源的因果、课设内容、岗位门槛、分数、理论或未登记待办 ID，不能因语气顺畅而通过事实门槛。R12 的今日简报和空输入匹配说明在可执行性、简洁度上仍有改进空间，见评分表。

AI 自评满足用户这次的评审执行方式；其独立性弱于外部真人评审。若产品发布仍要求独立真人评委，应另做复核，不能将本次记录改写为“真人盲评”。

## 失败迭代与中断记录

- 早期 R0 与候选测试先解决页面任务误注入对话 `guide.closing`、JSON 上限人为截断、契约强制虚构分数等问题。Prompt-R5 仍有一份诊断 JSON 修复后不完整；R6 的程序边界 282/282，但自评发现候选仍把“未回复”归因、断言岗位通则和把情绪当动机。
- R7～R10 收紧证据边界与复盘 `unknown` 归因。R10 全套 442 份候选结构均有效，新增的语义检查仍抓到 2 条“未回复意味着材料没对上/简历未被打开”。R11 定向重测又发现共享收尾提示词示例把“投简历没回音”带进无此事实的决策案例；R12 移除示例并明确只引用本轮输入。
- R12 首次尝试将主、扩展与 38 类高风险语料同时提交，DeepSeek 返回 `429 Too Many Requests`，随后返回 `402 Payment Required`；这次中断的 JSONL **作废，不能当作通过记录**。用户告知额度恢复后，先用单例验证 1/1，再依次重跑主语料、扩展语料与九个代表性高风险场景，得到上表的最终结果。
- 真正调用服务时，模型在图点正确的情况下曾把已知专业说成未知。明确要求画图时改用通过校验的图点生成确定性说明；图表项目名称不会误触发画图。此修复属于业务编排，不计作提示词本身的提升。后续又发现模型在黑板已有画像时口头称“没拿到画像”，编排层将已存画像字段摘要提升到本轮输入显眼位置，真实图表探针连续四轮各 8/8。
- 一轮浏览器首答提到内部“格式问题”，反映结构修复指令与用户可见回复串扰。编排器纠错输入现明确区分内部反馈与用户问题，重放原始任务上下文，并禁止在用户可见字段提及校验/重试。新增结构修复单测；DeepSeek 用同形纠错提示独立运行五次，5/5 有效且没有内部措辞泄露。这五次是**纠错提示专项探针**，不混入上表的 297 份 R0/R12 对照；偶发真实修复轮仍需继续观察。
- 最新真实五环节复测的复盘轮两次把已登记行动任务的机器 ID `t1` 写进用户可见文本。模型输入里保留 ID 供业务勾选，展示前根据已登记行动计划替换成带引号的任务正文，结构化 ID 不变。重启源码服务后，用原账号在复盘环节直问 `t1`，用户回复不再暴露 ID；这一改动是输出映射，未改 R12 的 26 条提示词正文或上表固定语料。

## 真实服务与范围

同一本地候选源码使用隔离 PostgreSQL `zhiyin_audit`、Redis 前缀 `zhiyin_audit`，DeepSeek 对话与 DashScope `qwen3.7-text-embedding-flash` 的 1024 维嵌入均有真实请求证据。真实服务与浏览器的最终轮次结果在《测试用例与结果矩阵.md》记录；未实现或未开放能力按《缺陷清单.md》列为例外。模型结果只支持上述合成样本和本地回归，不代表所有未来输入均不会出现隐性推断。
