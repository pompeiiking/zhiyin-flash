# 发布、版本与回滚记录

更新于 2026-09-25。本次完成后仅作**本地 Git 提交**，不推送、不发布。本文记录候选环境与可执行回滚步骤；开发测试不等于生产发布。

## 版本与环境指纹

| 项 | 当前事实 |
| --- | --- |
| 合并基线 | `origin/main` 与提交前本地 main 均为 `635fcb6274126642e59cf1881c4804d4c6fcc4e6`；2026-09-25 已重新 fetch 核对 |
| 唯一项目版本源 | `zhiyin-src/template/pyproject.toml` 为 `1.0.1`；前端包版本同步，OpenAPI `info.version` 已重新导出并核对 |
| 候选 API | 本地源码进程 `127.0.0.1:18000`；OpenAPI 44 路径、46 方法；健康状态 ok |
| 候选前端 | Vite `127.0.0.1:5174` 指向 `:18000`；`vue-tsc` 与 `vite build` 成功 |
| 数据隔离 | PostgreSQL `zhiyin_audit`、Redis key 前缀 `zhiyin_audit`；现有 Docker 业务服务与原数据库保留 |
| 旧镜像 | `:8000` 服务仍是旧版，15 路径/版本 0.1.0；不用于本次候选结论 |
| 模型 | 数据库路由 DeepSeek `deepseek-chat`；嵌入 DashScope `qwen3.7-text-embedding-flash`，1024 维 |
| 提示词 | 文件与隔离库 26/26 正文哈希、启停状态一致；运行 Agent 缓存按指纹更新 |

## 本次提交判据

按用户的版本规则，模块完成并稳定、同步最新 main 后，将 `pyproject.toml` 的 `1.0.m` 从 `1.0.0` 增至 `1.0.1`；本次未触及由用户决定的 `n`。匿名 A/B 由用户指定 Codex AI 自评，候选事实硬失败 0/10；全量单测、Ruff、OpenAPI、前端构建、真实服务和浏览器回归均记录在《测试用例与结果矩阵.md》。提交只在本地进行，**不执行 git push**。用户原有的《信息采集与快速回答问题定位.md》不纳入本次提交。

## 候选回滚方法（尚未执行生产演练）

1. 记录要回退的项目提交、`pyproject.toml` 版本、26 条提示词文件哈希、数据库注册表哈希、模型路由与 OpenAPI 指纹。
2. 在隔离/发布对应库中备份注册表和业务数据；用目标提交中的 `data/registry/prompts.json` 运行 `python -m zhiyin_boot --resync-registry --check --phase=1`，核对启停状态和 26 条正文，再重启服务以清 Agent 缓存。
3. 若需要回退代码，从已知通过的提交重新构建 API/前端镜像，确保 `openapi.json` 与运行 `/api/v1/openapi.json` 一致；不要直接回到当前旧 `:8000` 镜像，因为其接口只有 15 路径。
4. 用隔离账号验证健康、登录、五环节、九任务、图表、笔记与任务勾选，确认版本及缓存重新一致；再决定是否切换正式流量。

本次验证了隔离库注册表重新同步、源码服务启动与重启、五环节 35/35、九项页面任务先前三轮各 9/9 且 R12 一轮 9/9、浏览器 81/81、全量 pytest 477/477、Ruff/OpenAPI/前端构建通过。**正式镜像构建、生产切流和生产回滚演练尚未执行**，因此不能填写“已发布/已完成回滚”。
