# 职引：全功能深度测试、业务逻辑梳理与提示词版本迭代任务文档

> 状态：本次源码审计、已实现功能回归与提示词迭代已完成，未实现/关闭能力按用户决定列为明确例外；尚未正式发布。基线：`main` 提交 `635fcb6274126642e59cf1881c4804d4c6fcc4e6`，2026-09-24；2026-09-25 已重新核对远端 main 未变化。逐项结果见根目录《功能与代码映射.md》《测试用例与结果矩阵.md》《缺陷清单.md》《提示词审计与候选版本.md》《模型评测记录.md》《发布及回滚记录.md》。

## 1. 目标与完成判据

目标是把职引从入口到复盘的**所有可见功能及其支撑链路**逐一验证，形成代码实现图、缺陷证据、提示词层级审查和可回退的版本迭代方案。一次测试通过、模型能返回 JSON、或页面能显示，都不能单独代表业务闭环通过。

工作完成须同时满足：

1. 冻结同一源码提交、镜像、数据库迁移、配置快照、OpenAPI 指纹、前端构建和模型路由；测试环境与发布环境的差异有记录。
2. 本文第 3 节全部功能有**功能编号、用例、执行结果、证据链接、缺陷编号**。`openapi.json` 当前 44 路径、46 方法逐项覆盖；新接口自动纳入，废弃接口说明原因。每项至少验证正常、无权限/无效输入、状态变化与可读回执；纯只读项可用空数据和有数据两态替代状态变化。
3. 五环节、三个前端页面、七个任务入口、九项 AI 页面任务、26 条提示词、后台任务和数据联动均有正反例；跨模块链路检查持久化、缓存、版本、行为轨迹和页面显示的一致性。
4. 自动化测试、真实服务调用、接口端到端、浏览器端到端各至少进行一轮基线和一轮修复后回归；模型随机性高的场景另做重复运行。所有失败保留原始输入、匿名化输出、时间、版本和复现步骤。
5. 提示词候选版达到预先登记的质量阈值，在文件、数据库和运行 Agent 中确认同版生效；回归全部功能后再决定发布。若未达标，保留基线版与失败结论，不以主观印象宣布优化成功。

本任务的交付物：`功能与代码映射.md`、`测试用例与结果矩阵.md`、`缺陷清单.md`、`提示词审计与候选版本.md`、`模型评测记录.md`、`发布及回滚记录.md`。这些是后续执行产物；本文为总任务书。

## 2. 依据、已知基线与执行约束

- 产品规则：[`docs/职引-完整设计文档.md`](docs/职引-完整设计文档.md)；实现入口：[`orchestrator.py`](zhiyin-src/template/zhiyin-business/zhiyin_business/services/orchestrator.py)、[`ai_tasks.py`](zhiyin-src/template/zhiyin-business/zhiyin_business/services/ai_tasks.py)、[`agno_engine.py`](zhiyin-src/template/zhiyin-orchestration/zhiyin_orchestration/impl/agno_engine.py)。
- 接口清单以 [`openapi.json`](zhiyin-src/template/contracts/openapi.json) 和实际运行 `/openapi.json` 双向比对；前端以 [`client.ts`](zhiyin-src/template/zhiyin-web/src/api/client.ts)、[`router.ts`](zhiyin-src/template/zhiyin-web/src/router.ts) 和三个 View 为准；提示词以 [`prompts.json`](zhiyin-src/template/data/registry/prompts.json) 与实际数据库注册表双向比对。
- 前次基线：源码静态测试在隔离导入问题后 465 通过、1 失败（README 缺设计文档链接）；真实 DeepSeek、DashScope 嵌入、pgvector 检索可连通；当前 Docker 五环节脚本 27/35 通过。详见此前的《提示词与业务逻辑测试审查报告》。这些数字**不能替代本任务的全功能结果**。
- 当前 Docker 镜像落后于上述 main；`127.0.0.1:8000` 和 `localhost:8000` 曾暴露不同 OpenAPI。开始任何结果归因前，先消除或明确记录此差异，并以唯一地址测试。测试用独立账号和隔离数据；不得把真实用户资料或 API 密钥写入日志、文档、固定样本、截图或仓库。
- 设计文档中提出但被功能开关关闭、标为后续能力、或仅为演示稿的条目，也列入清单并标注状态为“已实现 / 条件启用 / 未实现 / 文档设想”。不能把未实现项算作测试通过；产品是否要求上线由需求确认。

### 2.1 项目版本号与协作规则

项目版本以 [`zhiyin-src/template/pyproject.toml`](zhiyin-src/template/pyproject.toml) 的 `[project].version` 为唯一依据；本次完成后为 **`1.0.1`**。每次形成新项目版本，都必须在对应提交中更新该字段，并使 PR 描述、测试记录、镜像与发布记录引用同一版本。其他位置若展示版本，应从这一依据生成或与其核对，不能各自维护一套数字。

- 固定采用 **`1.n.m`**：第一位 `1` 长期不变。`n` 是大版本，由项目负责人（用户）决定；底层或架构优化、大功能基座修改、业务逻辑方向变化属于申请大版本评估的情形。团队成员不得自行增加 `n`。
- `m` 是小版本。成员每天拉取并整合当日全部已合入更新后，只有在**自己负责的模块任务已完成、达到稳定状态、无须次日继续做同一模块**，准备提交当天最后一版 PR 时，才将当前 `m` 加 1。进行中的模块、未验证稳定的改动不能为了当天结束而升级版本。
- 多人并行时，每个待合并 PR 的目标版本必须基于**提交 PR 前最新的 main** 重新确认。若别人先合并并占用了同一版本，后合并者先拉取 main、解决冲突、完成回归，再把 `pyproject.toml` 中的 `m` 增至新的下一版并更新 PR；不能让两个合并提交都声明同一个版本。
- 例：当前 `1.0.0`。袁的已完成模块先以 `1.0.1` 合入；陶原本也准备 `1.0.1`，则拉取袁的提交并解决冲突，确认模块仍稳定后改为 `1.0.2`，再提交更新后的 PR。版本升级与 PR 合并顺序一致。
- 大版本 `n` 的具体目标值及变更后 `m` 的起点由项目负责人确定；任务文档不预设重置规则。若本次工作触及大版本情形，先在报告中列出变更范围与影响，等待负责人定版号。

本任务的提示词实验编号与 `1.n.m` **分开**：实验编号仅区分候选方案，不表示项目发布版本。每个最终被采纳并进入稳定模块 PR 的提示词变更，仍须按上述协作规则更新 `pyproject.toml`。T0、每次回归和最终报告都要记录实际项目版本；版本变更后重新核对接口、数据库迁移、提示词数据库值和运行实例。

## 3. 全功能覆盖矩阵

矩阵中的“接口”是当前契约中的**完整方法清单**。每组还须检查前端入口、业务服务、仓储、数据库副作用与异常回执。记录时为每个方法拆成独立用例行；本表不把多个方法视作一次覆盖。

| 编号 | 功能及必须检查的行为 | 当前接口（方法 路径） | 首要验收点 |
| --- | --- | --- | --- |
| F01 | 健康、启动装配、动态配置 | `GET /healthz`；`GET /api/v1/app/bootstrap`；`POST /api/v1/app/config/reload` | 依赖状态如实反映；注册表漂移、配置重载范围、未授权重载有证据 |
| F02 | 注册、登录、登出及会话隔离 | `POST /api/v1/app/auth/register`；`POST /api/v1/app/auth/login`；`POST /api/v1/app/auth/logout` | 令牌生效/撤销、错误凭据、跨账号数据隔离；前端登录浮层和退出状态一致 |
| F03 | 门户、任务入口、任务列表与历史 | `GET /api/v1/app/portal`；`POST /api/v1/app/task/enter`；`GET /api/v1/app/sessions`；`GET /api/v1/app/sessions/{task_id}/turns` | 七个入口均可进入；会话续接、逐轮原文、跨账号 task_id 拒绝 |
| F04 | 五环节对话、选项和材料 | `POST /api/v1/app/conversation/message`；`POST /api/v1/app/conversation/material` | collect→diagnose→decide→act→review；自由文本、`option_id`/`option_value`、文件回执、重复 `client_msg_id` 幂等、材料仅在授权会话使用 |
| F05 | 工作台、画像及外部学籍 | `GET /api/v1/app/workspace`；`POST /api/v1/app/chsi/bind` | 字段名称、来源、置信、缺口与展示一致；绑定失败、凭据保护、未绑定状态可理解 |
| F06 | 学业导入与删除 | `POST /api/v1/app/academic/import`；`POST /api/v1/app/academic/import/file`；`DELETE /api/v1/app/academic` | 文本和文件导入、课程/成绩解析、重复和坏文件、删除后画像与缓存回退、跨账号隔离 |
| F07 | 诊断报告及版本 | `GET /api/v1/app/report/full-text`；`POST /api/v1/app/report/summary`；`GET /api/v1/app/assets/{asset_type}/versions`；`POST /api/v1/app/assets/export` | 报告依据与理论引用可追溯；每个历史版本均有正文；重算告知、导出内容与所选版本一致 |
| F08 | 缺口、维度、画像分析和职业匹配 | `POST /api/v1/app/gaps/{gap_key}/clarify`；`POST /api/v1/app/dimensions/{dimension_id}`；`POST /api/v1/app/portrait/analysis`；`POST /api/v1/app/match/careers` | 缺数据不造分；补事实后重算；字段、匹配理由、来源与当前画像一致 |
| F09 | 决策方案与选择 | `GET /api/v1/app/plan/directions`；`POST /api/v1/app/plan/directions/{option_id}/select` | 主攻/平行/保底方案、代价、明确选定、改选、无效 option、选择后推进 |
| F10 | 行动计划、任务与日历 | `GET /api/v1/app/plan/action`；`PATCH /api/v1/app/plan/action/tasks`；`GET /api/v1/app/calendar`；`POST /api/v1/app/plan/timetable`；`POST /api/v1/app/plan/todos/suggestions` | 任务可执行且可撤回；勾选入库；节点与学期课表不冲突；行动变化使关联 AI 缓存失效 |
| F11 | 今日简报和日建议 | `POST /api/v1/app/brief/today`；`POST /api/v1/app/day/{day}/advice` | 时区边界、空状态、任务/学业改变后的失效重算、重复读取命中缓存、SSE 终帧完整 |
| F12 | 情报与理论知识 | `GET /api/v1/app/intel`；`POST /api/v1/app/intel/refresh`；`GET /api/v1/app/theory-cards/{theory_id}` | 缓存与主动刷新区分；事实来源、时间、新旧状态；无外部事实时不伪造引用 |
| F13 | 笔记全生命周期 | `GET /api/v1/app/notes`；`POST /api/v1/app/notes`；`PATCH /api/v1/app/notes/{note_id}`；`DELETE /api/v1/app/notes/{note_id}` | 新建、读取、编辑、删除、排序/空态、越权和输入边界 |
| F14 | 主动提醒与已读 | `GET /api/v1/app/notifications/pending`；`POST /api/v1/app/notifications/{message_id}/read` | 触发阈值、冷却、窗口限次、文案、去重、已读及跨账号隔离 |
| F15 | 行为时间线、成就 | `POST /api/v1/app/track`；`GET /api/v1/app/track/events`；`GET /api/v1/app/achievements` | 前端与后端事件、顺序、去重；规则解锁一次且可读；复盘依据真实行为 |

**前端全量走查。** `ConsoleView`、`PortalView`、`ReportView` 三页及 `/login` 重定向都要检查桌面和窄屏、加载/空态/失败态、登录状态变化、刷新恢复、键盘可用性和无虚构数据。逐屏核对首页任务卡、对话区及选项、材料上传、工作台画像/任务/日历、门户菜单与开关、报告全文/版本/导出、笔记、通知、理论卡、图表与时间线。用实际网络请求核对每个可见动作，不以静态图稿判定功能。功能开关 `report_full_text`、`export`、`calendar`、`achievements`、`mentor` 要分别验证开/关两态及对应前后端行为。

**无独立用户接口但必须覆盖的能力。** PostgreSQL 迁移和仓储、Redis 缓存/限流、对象存储、RAG 理论卡/文档切片/向量同步、DeepSeek 路由、DashScope 1024 维嵌入、Web 检索、学信网与学制网网关、计划调度、主动事件 Worker、数据源失败降级、加密与日志脱敏、动态注册表导入/漂移、部署与数据迁移。分别记录启用条件、输入输出、持久化位置、失败恢复和告警；外部服务不可用时不得显示假成功。

## 4. 业务逻辑梳理任务

输出一张“入口 → 控制器 → 服务 → 编排/模型或规则 → 仓储/外部服务 → 响应 → 前端呈现”的映射表，逐项对应 F01～F15；标注谁是事实来源，何处会改变数据，何处只是展示。至少梳理以下闭环：

1. **五环节主线：**七类任务入口与自由聊天如何定位阶段；采集门槛、主动意图、选项身份、主理交接、可视件和复盘进入条件如何判定；模型负责文字和结构，规则负责哪些状态决定。
2. **四类数据账：**画像事实及来源、对话/行为轨迹、诊断/方案/计划资产与版本、外部情报和理论引用。画出更新、影响面传播、待重算标记、真正重算、结论变化告知与回滚路径。
3. **页面任务链：**九项 AI 任务的入口、输入快照、提示词、模型路由、输出契约、SSE 帧、缓存键与失效条件；缺画像、缺事实、超时和模型失败时的展示。
4. **异步链：**行为事件、主动提醒、日历/学期节点、向量同步及信息刷新；记录触发条件、幂等键、重试、冷却、重复投递与可观测性。
5. **安全与身份：**账号、令牌、任务/笔记/资产归属、材料访问和敏感字段；验证每个读写接口的身份边界及错误状态。

每条链须同时有代码位置、状态图或时序图、数据库表/缓存键、正反例、当前测试覆盖和未覆盖点。设计文档与实现不一致时，以“设计预期 / 代码现状 / 处理决定”三列记录，不静默改成其中一种。

## 5. 多轮深度测试计划

| 轮次 | 执行内容 | 必须留下的证据 |
| --- | --- | --- |
| T0 版本冻结 | 以 `pyproject.toml` 的 `1.n.m` 为准，核对 git 提交、镜像 digest、前端构建、44/46 契约、实际 OpenAPI、数据库迁移、注册表和模型路由；固定唯一访问域名 | 项目版本及环境指纹、差异清单和可复现启动步骤 |
| T1 静态与单元 | 全量 pytest、Ruff、前端 typecheck/build、接口契约一致性、提示词结构/引用/消费者静态检查 | 原始日志、测试数、失败归因；不以导入故障掩盖业务断言 |
| T2 模块与契约 | F01～F15 每个方法的正常、异常、权限和空态；仓储契约、迁移、Redis、上传、SSE、配置开关 | 46 方法逐项结果、响应样本与 DB/缓存前后快照 |
| T3 真实依赖 | DeepSeek、DashScope、pgvector、RAG、Web、学籍/学业可用路径；故障/超时/限流/维度不匹配注入 | 请求与响应元数据、耗时、脱敏错误、召回证据和降级判断 |
| T4 跨功能端到端 | 五环节、七入口、资料导入、画像/报告版本、决策改选、任务完成、日历、复盘、通知、导出、笔记、成就与缓存传播 | 接口轨迹、逐步断言、资产和行为表快照；现有 `loop_verify.py` 的 35 项全部复跑 |
| T5 浏览器端到端 | 三页面逐屏、桌面/窄屏、登录/退出、任务切换、上传、选项、图表、报告版本、失败态；运行现有 `full_path.py` 等浏览器探针并补缺口 | 截图/录屏、网络请求、控制台错误、前后端状态比对 |
| T6 稳定性与回归 | 并发多用户、重复提交、断网恢复、长会话、跨日时区、大文件、限流、重启后续接；修复后全量复跑 | 资源/时延分布、重复率、数据一致性、回归报告 |

执行时为每个功能建立 `案例 ID / 前置状态 / 输入 / 预期 API / 预期 DB 与缓存 / 预期 UI / 实际 / 证据 / 缺陷`。失败分成环境版本、业务规则、数据/缓存、接口契约、前端、模型输出、外部依赖七类。对多次深度测试，至少执行“基线 → 定位修复 → 全量回归”；模型评测另按第 6 节进行多次重复与对照。真实调用的成本、速率与测试账号清理方式在 T0 记录。

## 6. 提示词全层级审计与评测

审计对象为当前 26 条配置，按 `code → 文件值 → 数据库值 → 调用点 → 角色 → 上下文 → 参数 → 输出契约/展示面 → 缓存 Agent → 实测结果` 建表，任何缺失调用点单独标记。不得只看配置正文。

| 层 | 代码 | 重点检查 |
| --- | --- | --- |
| 总纲/收尾 | `core.system`、`guide.closing` | 真实性、证据、用户自主权；对话与页面任务共用时是否注入无关指令 |
| 六个角色 | `role.profile_analyst`、`role.career_advisor.diagnose`、`role.career_advisor.decide`、`role.path_planner.act`、`role.companion_coach.review`、`role.info_scout` | 职责边界、交接、事实与建议区分、检索权限、阶段口吻 |
| 路由及告知 | `router.clarify`、`disclosure.lead_change`、`disclosure.reason.collect/diagnose/decide/act/review`、`disclosure.conclusion_change` | 是否实际消费、触发时机、用户是否看见、结论变化是否准确 |
| 主动提醒 | `flow.proactive` | 与实际 `notify.stall.*` 投递文案和 Worker 触发点对齐；不施压、不编造进展 |
| 九项页面任务 | `task.brief.today`、`task.dim`、`task.portrait.analysis`、`task.day.advice`、`task.gap`、`task.report.summary`、`task.plan.timetable`、`task.plan.todos`、`task.match.careers` | 输入缺失、长度/字段、结构化契约、缓存、来源与页面展示；特别核对 `task.plan.todos` 顶层 `items` 对象 |

**固定评测集。** 至少包含：每环节正常/信息不足/情绪性表达/矛盾事实各 2 例；七入口各 1 例；九项页面任务各 3 例（完整、缺数据、冲突数据）；外部事实过期或失败、引用不可用、用户改选、任务停滞、重复点击、跨日缓存和材料污染各 2 例。每例保存匿名输入快照、期望事实边界和业务断言，不把基线模型输出直接当唯一正确答案。

**重复与对照。** 每个候选提示词在固定模型和参数下对同一评测集至少独立运行 3 次；高风险场景（虚构分数/引用、情绪性回应、决策越权、结构化失败）至少 5 次。基线版与候选版交错执行，记录提示词指纹、模型请求名及响应名、参数、输入、输出、耗时、token/费用和契约校验。人工盲评覆盖相关性、尊重用户、具体可执行性与表达清楚程度，评审人与版本名分离。

**硬门槛。** 合约有效率 100%；不可凭空编造用户经历、学历成绩、职业分数或外部引用；不能替用户直接确定不可撤回方向；真实字段变更及引用必须能追到来源。比较指标另包括有效下一步比例、重复追问率、无依据结论率、条目数量与稳定性、平均及 P95 时延、费用。阈值在运行候选版前锁定；任一硬门槛失败即不得发布该候选版。

## 7. 提示词方案迭代与项目版本对应

下表 `Prompt-R*` 是评测方案编号，只用于比较提示词；**不等同于** `pyproject.toml` 中的项目版本。方案完成并作为稳定模块合入时，实际 `1.n.m` 依第 2.1 节和 main 合并顺序确定，不能预先把 `Prompt-R1` 写成项目 `1.1.0`。

| 方案编号 | 任务 | 出口条件 |
| --- | --- | --- |
| Prompt-R0 基线 | 冻结 26 条文件/数据库/运行值、调用链和固定样本；记录当前模型、契约、缓存与环境 | 能重放基线并解释文件、数据库、Agent 三层差异 |
| Prompt-R1 契约对齐 | 修正 `task.plan.todos` 的数组/对象矛盾、转义残留；给无调用点配置明确去留；对齐 `flow.proactive` 与通知实际路径；核对 `params` 的实际消费 | 静态检查无悬空启用项；九项任务结构与契约一致 |
| Prompt-R2 层级重构 | 总纲只保留共话和九项页面任务分别定义输入、边界、输享底线；五环节对出与缺数据规则；交接、引用和主动提醒独立归位 | 固定评测集达到硬门槛，用户界面无无关聊天指令 |
| Prompt-R3 发布可追溯 | 增加提示词版本/指纹、模型路由、契约版本和参数日志；Agent 缓存按版本失效；数据库导入、实际生效和回滚流程可验证 | 灰度对照通过，三层版本一致，回滚演练成功，T1～T6 无阻断缺陷 |

每版产出逐条变更理由、前后文案、受影响功能编号、对照数据、失败样本、评审结论和回滚包。先修清业务与部署差异，再解释提示词效果；不得将 Docker 旧镜像结果算作 main 提示词效果。

## 8. 当前已知问题与优先级

1. **P0 环境一致性：**当前 Docker 镜像与 main 源码不同，且本机 `127.0.0.1`/`localhost` 曾暴露不同 API；T0 必须先固定入口和版本。
2. **P1 现有闭环失败：**旧镜像的 35 项回路中 8 项失败，涉及空历史版本、影响面/结论告知、决策推进、任务与学业变更后的缓存、复盘推进、重复消息。统一版本重跑后逐项归因。
3. **P1 提示词配置：**`task.plan.todos` 与契约冲突；`flow.proactive` 有配置但实际提醒走模板；Agent 缓存与配置版本未绑定。真实小样本通过不能取消这些风险。
4. **P2 可追溯性：**嵌入路由标识为 `doubao`，实际地址和模型是 DashScope；参数声明与消费点不完全一致；根 README 的设计文档链接断言失败。

优先级是本任务的排查顺序，不预设所有问题仍存在于统一版本上。每一项最终都要有“已复现 / 已修复 / 版本差异 / 需求决定不做”之一及证据。

## 9. 最终验收与报告格式

最终报告首页给出：`pyproject.toml` 中的项目版本及对应提交、F01～F15 覆盖率、46 方法逐项结果、五环节与九任务结果、T1～T6 通过/失败数、阻断缺陷、Prompt-R* 方案对照及是否建议发布。附录包含匿名测试数据、环境指纹、命令、原始日志、网络轨迹、数据库与缓存快照、截图/录屏、缺陷复现步骤和回滚验证。每个拟合并 PR 须附当前 main 版本、目标版本、模块完成与稳定性说明、冲突同步记录及回归证据。

只有当全部在范围内的功能均有执行结果、P0/P1 缺陷关闭或有明确产品裁决、提示词硬门槛通过、同版环境全量回归通过，才标记“全功能验收完成”。未实现或条件关闭的功能必须明确列在例外表，不能从分母里悄悄删除。

## 10. 2026-09-25 执行核对

已按 `main@635fcb6` 加本地候选修改逐项执行 F01～F15 与 OpenAPI 44 路径、46 方法；全量 pytest 477/477、Ruff、OpenAPI 快照、前端类型检查与构建通过。隔离 PostgreSQL/Redis 上的真实服务五环节 35/35、九项页面任务 9/9、非模型接口 43/43；浏览器 81/81、图表连续四轮各 8/8，详见《测试用例与结果矩阵.md》。

最终提示词实验为 Prompt-R12：主语料 201/201、扩展 51/51、九个高风险案例各五次 45/45，候选合计 297/297 结构有效、可程序判定事实边界零违例。R0 对照 296/297。根目录《盲评样本与评分表.md》十组匿名 A/B 已评分。用户随后明确指示由 Codex 自行判断，故本次评审方式为 **AI 自评**，并未发生独立真人评审；候选 77/80、事实硬失败 0/10，基线 69/80、事实硬失败 6/10。独立真人复核可作为后续发布决策，不得在本报告写成已完成的真人盲评。

用户决定本次不补齐尚未实现或关闭的能力。导出、导师、学信有效码正向绑定、前端功能开关门控、限流端点接线、信息侦查员独立调用及普通账号可重载配置等，均在《缺陷清单.md》明确列为例外、条件项或后续风险，未写成“功能通过”。正式旧 Docker 镜像替换、生产切流与生产回滚不属于此次本地提交；本地源码及隔离服务是本次验收依据。

最终完成状态以《测试用例与结果矩阵.md》《模型评测记录.md》《发布及回滚记录.md》的同版结果为准。提交前再次核对 `origin/main` 与 `pyproject.toml`，完成小版本递增；仅执行本地提交，不推送。
