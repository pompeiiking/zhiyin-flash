# 缺陷、限制与例外清单

更新于 2026-09-25。编号用于关联《测试用例与结果矩阵.md》；“已修复”仅指本地候选源码，尚未发布。按用户决定，当前未实现或关闭的能力**本次不补齐**，列为明确例外。

| ID | 级别 | 现象与证据 | 本次处理 / 余留 |
| --- | --- | --- | --- |
| D01 | P0 环境 | 原有 Docker `:8000` 暴露旧版 API（15 路径，版本 0.1.0），与 `main` 的 44 路径/46 方法不一致；旧镜像五环节 27/35 | 已用隔离库启动最新源码 `:18000`，五环节 35/35；旧容器保持原状。正式发布前必须重新构建镜像并核对指纹 |
| D02 | P1 已修复 | 图表工具把 0–1 的把握度乘成 90，前端又乘 100，显示比例会扩大百倍 | 工具统一输出 0–1，后端校验范围；前端显示 90%；单测、真实模型五轮图表与浏览器均通过 |
| D03 | P1 例外 | 功能开关在 bootstrap/portal 回包中，但前端没有直接消费 `feature_flags`；例如 `export=false` 时报告页仍显示“导出”按钮，点击后才回告不可用 | 按用户决定本次不补齐；后续需定义关闭态的前端门控与验收。当前不能宣称五个开关的开/关两态全部正确 |
| D04 | P1 例外 | `POST /app/config/reload` 只检查已登录，普通账号可触发进程动态配置重载；代码注释承认管理员角色尚未签发 | 隔离环境验证未登录 401、已登录可重载；多用户正式部署前应加管理员授权。本次不改权限模型 |
| D05 | P1 已修复 | 明确要求看图时，模型在行动/复盘环节偶尔没有调 `chart.render`，接口无图；此前浏览器两轮失败 | 服务端仅在明确图表请求时从已存画像字段补图，少于两项不给图；真实图表五轮、浏览器 81/81 |
| D06 | P1 已修复 | `task.plan.todos` 提示词要求顶层数组，与输出契约顶层 `items` 对象冲突；`flow.proactive` 启用却无模型调用点 | 改为对象结构；无消费者的主动提醒提示词停用，实际通知继续由 Worker+文案模板生成；九项页面任务三轮通过 |
| D07 | P1 已修复 | `core.system` 原含对话口吻与选项规范，九项页面任务也注入 `guide.closing`；提示词动态更新后 Agent 缓存仍可能用旧指令 | 总纲只留共通事实/契约底线，对话规范归到 guide；页面任务不注入 guide；缓存键包含实际提示词/角色参数指纹，运行日志记录指纹和契约；单测覆盖热更新 |
| D08 | P2 已修复 | 提示词 `params` 中的超时、重试声明未传给运行模型工厂；画像分析提示词含字面 `\n` | 参数注入实际模型，移除转义残留；单测与真实调用通过 |
| D09 | P2 例外 | `POST /app/assets/export` 仅预留入口，恒返回 `available=false` | 未假装导出成功；本次不实现 PDF/DOCX 导出 |
| D10 | P2 例外 | `mentor` 功能开关关闭，无可用导师产品链路 | 本次不补齐，后续由产品定启用时点和流程 |
| D11 | P2 条件 | 学信网绑定无有效在线验证码，只验证无效码返回错误 SSE，不写假学籍 | 正向绑定待真实、授权的测试验证码；不采集用户账号密码 |
| D12 | P2 已补测 | 初次巡检只验证不存在 ID 返回 `read=0` | 使用隔离库真实待处理通知：本人已读后待处理数 2→1，跨账号 `read=false`；Worker 停滞触发与冷却由 `test_main_path.py` 覆盖 |
| D13 | P2 已按用户指定方式评审 | R12 主语料 201/201、扩展 51/51、九个高风险例五轮 45/45，合计 297/297 合约有效且程序可判定边界零违例；匿名 A/B 10 组已评分 | 用户明确要求由 Codex 自行判断，故评审者为 AI 自评，不能写作独立真人盲评；R12 77/80、硬失败 0/10，R0 69/80、硬失败 6/10。若发布流程仍要求独立真人评委，需另做复核 |
| D14 | P2 已补测 | T6 原缺断网、跨日时区、大文件、限流、长会话与故障注入 | 已补浏览器断网恢复、超限文件拒绝、并发 50/50、幂等、重启续接、跨日/时区缓存键、22 轮会话；外部数据与向量失败降级有单测。持续数小时压测不属于“长会话”，若需另列性能专项；限流属未接线例外 D15 |
| D15 | P2 例外 | Redis 限流网关已装配，源码中未发现业务控制器/服务调用其 `allow`；因此单元网关限额通过不代表 API 请求已经限流 | 本次按“未接线能力”报告，不为避免误判而把单元通过写成线上限流通过；后续需确定哪些端点需要配额 |
| D16 | P1 已修复并自评 | 诊断输出会从课设名称推断出完整建模流程；方向方案契约强制填写 `match_score`，画像维度在缺证据时把未知分数、历史差值和基准写成 0，页面显示成真实分数或“与上次持平” | 无依据分值改为 `null`，页面显示“待核验”并跳过无分值图点；提示词禁止从名称推断技能或用 0 代替未知。R12 结构及程序边界 297/297，真实五环节 35/35；匿名自评 10 组零硬失败。未来输入的隐性推断仍需持续监测 |
| D17 | P2 例外 | `role.info_scout` 已登记为角色配置，但主流程没有独立调用该角色；外部事实由数据源与工具获取 | 本次记录配置与实际调用点的差异，不把外部检索成功记作信息侦查员提示词通过；后续由产品确定是否启用该角色 |
| D18 | P1 已修复 | 真实图表五轮的最后一轮，图点从已存画像正确读取，模型文字却说“不知道你的专业”，与已存“专业”字段冲突 | 明确请求图表时，对话文字改为根据通过校验的图点确定性生成；缺足够图点时明确说明无法绘图。增加文字与图点一致性断言；真实服务图表探针 7/7（含项目名称不误触发）及浏览器回归已通过 |
| D19 | P1 已修复 | 图表探针一轮在数据库已有三项画像字段时，主理却说“没拿到你的画像”，与共享状态矛盾 | 编排调用把已存字段及来源摘要置于本轮输入显眼位置；探针新增“已有画像时不说没有画像”断言，新增断言后连续四轮各 8/8 通过，后端同版单测 477/477；浏览器最终回归以《测试用例与结果矩阵.md》为准 |
| D20 | P2 已修复，持续观察 | 一轮真实浏览器首答在结构修复后说“上一轮我这边出了格式问题”，把内部校验反馈带进用户回复 | 纠错调用明确标注内部反馈非用户消息，带回原始用户任务与上下文，并禁止用户可见回复提及格式、校验、重试或 JSON；结构修复单测通过，DeepSeek 独立模拟纠错提示五轮 5/5 无泄漏。真实浏览器最终轮 81/81，仍需监测未来偶发修复轮 |
| D21 | P2 已修复 | 最新真实五环节虽然 35/35，但复盘回复两次把行动任务的机器 ID `t1` 直接说给用户 | 复盘产出用已登记的行动任务文本替换用户可见正文中的机器 ID，结构化 `task_id` 和成就 ID 保持原值；针对性单测与重启后的真实服务对话均确认 `t1` 不外露、任务名称可读 |

## 产品决定与提交门槛

2026-09-25 用户决定：当前未实现或关闭的能力先不补齐，本次任务后写报告列出。D03、D04、D09～D11、D15、D17 因此保留为公开例外或条件项，不把它们记作“已通过的完整功能”。D13 与 D16 涉及已实现功能的事实边界及评测，已按用户随后明确指定的 AI 自评方式执行并如实标注评审者；这不能替代未来可能要求的独立真人复核。正式旧镜像替换、生产回滚和有效学信码正向绑定仍是发布前条件或另行授权项。
